epic: v11-e11-2
entries:
  # In-scope: FCS scoped to projects (E11.2)
  - req: REQ-fcs-scoped-to-projects-create-fcs-assessment
    lld: lld-v11-e11-2-fcs-scoped-to-projects.md#LLD-v11-e11-2-fcs-create-api
    issue: 411
    files: []
    status: Revised
    # LLD corrected by issue #411 (pipeline path, enforcePerRepoAdmin signature, auth order).
    # files: populated by /feature-end after merge.
    # Rev 2 (v11-requirements 1.3): new AC for post-create polling on the assessment-detail page —
    # already implemented under issue #444 before rev 1.3 finalised. No new work; verification only.

  - req: REQ-fcs-scoped-to-projects-new-assessment-page
    lld: lld-v11-e11-2-fcs-scoped-to-projects.md#LLD-v11-e11-2-new-assessment-page
    issue: 413
    files: []
    status: Revised
    # LLD corrected by issue #413 (auth ordering, org scoping, single readMembershipSnapshot round-trip).
    # files: populated by /feature-end after merge.

  - req: REQ-fcs-scoped-to-projects-project-scoped-assessment-list
    lld: lld-v11-e11-2-fcs-scoped-to-projects.md#LLD-v11-e11-2-fix-441
    issue: 441
    files:
      - src/app/(authenticated)/projects/[id]/page.tsx
      - src/app/(authenticated)/organisation/assessment-overview-table.tsx
      - src/app/api/assessments/helpers.ts
      - src/app/(authenticated)/organisation/load-assessments.ts
      - src/app/(authenticated)/organisation/page.tsx
      - src/app/(authenticated)/organisation/deleteable-assessment-table.tsx
    status: Revised
    # Issue #414 built a bespoke card list (assessment-list.tsx, now deleted).
    # Issue #441 corrected that: project dashboard now uses AssessmentOverviewTable.
    # Rev 2 (v11-requirements 1.3): actions column parity with org list — replace direct
    # AssessmentOverviewTable render with DeleteableAssessmentTable (delete + view-detail icons).
    # See lld-v11-e11-2 §Pending changes — Rev 2.

  - req: REQ-fcs-scoped-to-projects-my-pending-assessments
    lld: lld-v11-e11-2-fcs-scoped-to-projects.md#LLD-v11-e11-2-pending-queue
    issue: 415
    files:
      - src/app/(authenticated)/assessments/page.tsx
      - src/app/(authenticated)/assessments/project-filter.tsx
    status: Revised
    # Rev 2 (v11-requirements 1.3): empty-state copy must communicate participant-only scope;
    # admin-no-participant case now has explicit AC. Query is unchanged (already participant-scoped).
    # See lld-v11-e11-2 §Pending changes — Rev 2.

  - req: REQ-fcs-scoped-to-projects-filter-pending-by-project
    lld: lld-v11-e11-2-fcs-scoped-to-projects.md#LLD-v11-e11-2-pending-queue
    issue: 415
    files:
      - src/app/(authenticated)/assessments/project-filter.tsx
    status: Revised

  - req: REQ-fcs-scoped-to-projects-project-scoped-assessment-urls
    lld: lld-v11-e11-2-fcs-scoped-to-projects.md#LLD-v11-e11-2-route-migration
    issue: 412
    files:
      - src/app/(authenticated)/projects/[id]/assessments/[aid]/page.tsx
      - src/app/(authenticated)/projects/[id]/assessments/[aid]/results/page.tsx
      - src/app/(authenticated)/projects/[id]/assessments/[aid]/submitted/page.tsx
      - src/app/(authenticated)/projects/[id]/assessments/[aid]/answering-form.tsx
      - src/app/(authenticated)/projects/[id]/assessments/[aid]/assessment-admin-view.tsx
      - src/app/(authenticated)/projects/[id]/assessments/[aid]/assessment-source-list.tsx
      - src/app/(authenticated)/projects/[id]/assessments/[aid]/load-assessment-detail.ts
      - src/app/(authenticated)/organisation/assessment-overview-table.tsx
      - src/app/(authenticated)/organisation/load-assessments.ts
      - src/app/api/assessments/helpers.ts
    status: Implemented

  # Cross-epic story carried by E11.2 route migration:
  # legacy `/assessments/[aid]` returns 404 (Story 4.5 deep-link compatibility).
  - req: REQ-navigation-and-routing-deep-link-compatibility
    lld: lld-v11-e11-2-fcs-scoped-to-projects.md#LLD-v11-e11-2-route-migration
    issue: 412
    files:
      - src/app/(authenticated)/projects/[id]/assessments/[aid]/page.tsx
    status: Implemented
    # Primary epic for navigation is E11.4; this entry duplicates the row in
    # coverage-v11-e11-1.yaml's Deferred block once E11.4 has its own manifest.
