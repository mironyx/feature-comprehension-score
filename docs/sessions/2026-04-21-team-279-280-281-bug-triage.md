# Team Session — 2026-04-21: Bug Triage (issues #279, #280, #281)

## Issues shipped

| Issue | Story | PR | Branch | Merged |
|---|---|---|---|---|
| #279 | fix: tool-use path missing response_format JSON constraint | [#283](https://github.com/mironyx/feature-comprehension-score/pull/283) | feat/tool-loop-response-format | 2026-04-21 |
| #280 | fix: mark tool-loop malformed_response errors as retryable | [#285](https://github.com/mironyx/feature-comprehension-score/pull/285) | feat/tool-loop-retryable | 2026-04-21 |
| #281 | fix: assessments list not polling during rubric generation | [#284](https://github.com/mironyx/feature-comprehension-score/pull/284) | feat/assessments-polling-fix | 2026-04-21 |

## Cross-cutting decisions

**Pre-existing MD041 violation in `docs/requirements/bug-report-21-04-26.md`** — all three teammates independently discovered and fixed the same missing H1 heading (from commit `92057f0` on main). Each chose slightly different wording, causing rebase conflicts on #280 and #281 after #279 merged first. This cost two extra CI cycles (~12 min combined) and added rebase complexity. The fix should have been a single `chore:` commit on main before spawning parallel agents.

**MD028 in `lld-v2-e17-agentic-retrieval.md`** — introduced by PR #283 (teammate-279's LLD sync). Teammate-281 caught it during their CI re-run and fixed it with a `markdownlint-disable-next-line` directive. Not caught earlier because LLD sync commits do not always re-run the full lint suite.

**Board status gap** — issues were added to the board via `scripts/gh-project-status.sh add` from inside worktrees, but appeared as Todo rather than In Progress. Root cause: the `add` command sets the status field correctly but the board's visual column was not reflecting it. A subsequent `gh-project-status.sh <N> "in progress"` (update, not add) fixed it. The `add` + status path may have a timing or field-ID issue worth investigating.

## Coordination events

- Wave: single wave, all three teammates spawned in parallel.
- Lead forwarded `/feature-end` to each teammate after user approval — correct protocol followed.
- Teammate-279 shut down late (forgot to send shutdown_request immediately after feature-end complete message). Fixed when user flagged it.
- Teammate-281 did not proactively report completion — required a lead ping to surface the result. Teammates should always send a final "feature-end complete" message to the lead without waiting to be asked.

## What worked / what didn't

**Worked:**
- Parallel implementation of three independent bug fixes completed in ~45 min wall time.
- All three fixes were surgical and correct — no review feedback, no logic errors.
- Each teammate independently ran TDD, evaluator, and CI probe without lead intervention.

**Didn't work:**
- Pre-existing broken doc on `main` cascaded into three separate CI failures and two rebase cycles. Expensive and avoidable.
- Lead did not notice the doc issue during issue validation — it was only caught at CI time.
- Teammate-281 went silent after feature-end without reporting back; required a ping.

## Process notes for `/retro`

1. **Scan main for lint/md violations before spawning parallel agents.** A single `npx markdownlint-cli2 "**/*.md"` run at `/feature-team` Step 1 would catch pre-existing breaks and allow a single fix commit before worktrees are created — avoiding the N×rebase cascade.
2. **Teammates must send a completion message to the lead after `/feature-end`.** The current skill prompt says "Report back… and wait" at PR creation time, but does not explicitly require a completion report after `/feature-end`. Add this to the teammate prompt.
3. **`gh-project-status.sh add` + status may not visually update the board column.** Prefer running `add` (default Todo) then immediately `<N> "in progress"` as two calls, or investigate whether the `add` + status path is reliable.
