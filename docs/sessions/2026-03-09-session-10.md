# Session 10 — 2026-03-09

## Completed work

- Phase 0.5.3: CI/CD pipeline with GitHub Actions (issue #17)
- GitHub Actions CI workflow with staged jobs: quality, unit tests, build, Docker
- Multi-stage Dockerfile for Next.js standalone output (Node 20 Alpine, non-root user)
- Added `markdownlint-cli2` with relaxed config for existing docs
- Scaffolded (commented) stages for integration tests, E2E, hadolint, trivy, SonarCloud
- PR #20 opened

## Files created

| File | Purpose |
| ---- | ------- |
| `.github/workflows/ci.yml` | CI workflow — quality, unit-tests, build, docker jobs |
| `Dockerfile` | Multi-stage Next.js standalone build (Node 20 Alpine) |
| `.dockerignore` | Excludes node_modules, .next, tests, docs from Docker context |
| `.markdownlint-cli2.yaml` | Relaxed markdownlint config for existing docs |
| `public/.gitkeep` | Empty public directory for Docker build |

## Issues encountered

| # | Issue | Fix |
| - | ----- | --- |
| 1 | markdownlint defaults produced 2721 errors on existing docs | Created `.markdownlint-cli2.yaml` disabling rules with widespread violations (line-length, table style, blanks around lists/fences/headings) |
| 2 | No unit tests exist yet (only integration tests) | Added `--passWithNoTests` flag to vitest in CI |
| 3 | Docker build failed — no `public/` directory | Created `public/.gitkeep` |
| 4 | Node 18 produced engine warnings (supabase-js, vitest, markdownlint all require Node 20+) | Bumped Dockerfile and CI to Node 20 |

## Decisions made

- **Node 20 in CI and Docker** — Multiple dependencies require Node 20+. Local dev still uses Node 18 (WSL default) which works but produces warnings. CI and Docker use Node 20.
- **markdownlint rules relaxed** — Disabled 13 rules with existing violations. Intended to tighten incrementally in a cleanup PR.
- **Docker build in CI, no push** — Validates the Dockerfile works without needing a registry or GCP project. Deployment deferred.
- **Security scans scaffolded** — hadolint, trivy, SonarCloud added as commented job definitions. Activate when ready.

## Board state

- Issue #17 in progress, PR #20 open
- Awaiting CI pass and review

## Next session should start with

1. Check CI pipeline result on PR #20 — fix any failures
2. Merge PR #20 after CI passes
3. Consider bumping local Node to 20 (align with CI/Docker)
4. Move to next Phase 0.5 issue — check project board for priority
