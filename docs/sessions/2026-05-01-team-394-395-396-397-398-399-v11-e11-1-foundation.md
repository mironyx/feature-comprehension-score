# Team Session — V11 E11.1 Project Management Foundation

**Date:** 2026-05-01
**Lead:** team-lead (feature-team skill)
**Epic:** #393 — V11 E11.1 Project Management foundation
**Teammates:** teammate-394, -395, -396, -397, -398, -399

---

## Issues shipped

| Issue | Story | PR | Branch | Status |
|-------|-------|----|--------|--------|
| #394 | T1.1 Schema | #400 | feat/v11-e11-1-schema | Merged |
| #395 | T1.2 Snapshot + gate | #402 | feat/v11-e11-1-snapshot-gate | Merged |
| #396 | T1.3 POST+GET API | #403 | feat/v11-e11-1-api-create-list | Merged |
| #397 | T1.4 GET+PATCH+DELETE API | #404 | feat/v11-e11-1-api-read-edit-delete | Merged |
| #398 | T1.5 List + new pages | #406 | feat/v11-e11-1-pages-list-new | Merged |
| #399 | T1.6 Dashboard + inline edit | #407 | feat/v11-e11-1-dashboard-inline-edit | Merged |

---

## Cross-cutting decisions

- **MSW enforcement mid-run:** teammate-395 used `fetchImpl` instead of MSW (the LLD prescribed `fetchImpl?: typeof fetch` in `ListAdminReposDeps`). Root cause traced to the LLD interface definition. Fixed in three places: `feature-core` Step 4b (test-author constraint), `feature-core` Step 6b (evaluator blocker), and `lld` skill Guidelines (no fetchImpl in `*Deps` interfaces). Cost: $9.73 extra on #395 for post-PR MSW migration.

- **Coverage manifest `issue:` field:** added mid-run (after Wave 2). ADR-0026 schema updated; `feature-end` Step 6.4 now writes `issue: <N>` alongside `files:` and `status:`. Wave 3+ teammates picked this up automatically.

- **LLD critical evaluation guidance:** strengthened `feature-core` Step 3b from "deviation permitted if over-engineered" to "critically evaluate — deviation is expected and welcome." Both Wave 4 teammates exercised this (inline error over toast, direct Supabase query over service layer).

- **Wave 3 merge coordination:** teammate-397 pre-emptively included both `CreateProjectSchema` and `UpdateProjectSchema` in `validation.ts` since #396 hadn't merged. `feature-end` Step 3.5 handled the rebase cleanly.

- **Step 6.4/6.5 disambiguation:** lead ran step 6.5 (epic checklist) when user asked for step 6.4 (coverage manifest) — the skill had just gained step 6.4 via a pull. Clarified and corrected.

---

## Coordination events

- Wave 1 (#394) — solo, gate for all others. Merged before Wave 2 spawned.
- Wave 2 (#395) — solo, gate for API endpoints. $9.73 post-PR cost from MSW migration caught in review.
- Wave 3 (#396 ∥ #397) — parallel, file-disjoint. #396 merged first to resolve `validation.ts` overlap. feature-end for both was already complete before lead forwarded the signal.
- Wave 4 (#398 ∥ #399) — parallel, file-disjoint. Both feature-ends already complete before lead forwarded. Coverage manifest updated by both; no conflicts.
- Overnight pause between Wave 2 review and Wave 3 spawn — teammates stayed idle, no token waste.
- Several process improvements committed to main mid-run: MSW callouts (skill), `issue:` field (ADR-0026 + feature-end), LLD critical evaluation (feature-core), delete-project Mermaid fix.

---

## What worked / what didn't

**Worked:**
- 4-wave dependency structure kept file conflicts to one (validation.ts, resolved by merge order).
- Teammates ran feature-end autonomously before receiving the lead's forwarded signal — zero blocking wait on lead.
- Coverage manifest `Revised` status correctly tracked deviations found by lld-sync.

**Didn't work:**
- LLD prescribed `fetchImpl` as a test seam — agent followed it literally. Cost $9.73 to fix post-PR. Prevention: LLD skill now prohibits this pattern.
- Step numbering confusion (6.4 vs 6.5) after a mid-session skill pull — lead ran the wrong step. Low impact but worth noting.

---

## Process notes for `/retro`

- LLD quality gap → test seam mismatch is a recurring cost driver (happened in #395). The lld-skill fix should prevent recurrence, but worth tracking over next 2–3 epics.
- Teammates completing feature-end before the lead signals is now the norm — the skill's "wait for lead" instruction is vestigial. Consider removing it or making it optional.
- `issue:` field in coverage manifest was identified and added mid-run by the human. Consider adding a pre-run LLD review step to the epic kick-off that checks manifest schema completeness.
- Wave 4 cost was notably lower than Wave 2 ($6.84 + $10.10 vs $13.62) — the MSW and critical-evaluation fixes improved quality before Wave 4 started.
