# Team Session Log — 2026-04-27 — Issues #358, #376, #377

## Issues shipped

| Issue | Story | PR | Branch | Merged at |
|-------|-------|----|--------|-----------|
| #358 — finalise_rubric org_id scope | ADR-0025 defence-in-depth | #378 | `feat/fix-finalise-rubric-org-scope` | 2026-04-27 |
| #376 — assessment detail self-fetch | Fix ERR_INVALID_URL 500 | #380 | `feat/fix-assessment-detail-self-fetch` | 2026-04-27 |
| #377 — RetryButton → org view | Correct retry placement | #379 | `feat/fix-retry-button-org-view` | 2026-04-27 |

## Cross-cutting decisions

- **#358 scope expansion:** Teammate-358's PR review caught a deeper atomicity gap — the `assessment_questions` INSERT ran before the ownership guard, so a wrong `p_org_id` could produce orphaned rows even without hitting the UPDATE. Both overloads now raise an exception upfront, making all writes atomic. Not in the original issue spec.

- **#377 `PollingStatusBadge` usage:** The issue body incorrectly characterised `PollingStatusBadge` as "only used from the My Assessments page today". It is also used in `create-assessment-form.tsx`. The teammate's PR removed retry from both, inadvertently breaking the creation flow. Caught during lead review; fixed with a targeted post-PR commit before merge.

- **`anti-patterns.md` DRY entry:** Added to the shared checklist based on the pattern discovered in #376 (and first seen in #132): implementing new query/service logic without checking for existing code in API routes or sibling modules. Committed directly to main post-merge.

## Coordination events

- All three teammates spawned simultaneously; #358 completed first (~18 min to PR).
- Teammate-377 crashed after CI passed but before `/feature-end` — context exhaustion after ~3 hours. Crash recovery ran from the lead pane: orphaned worktree detected, JSONL history read, lld-sync and session log written, PR rebased and merged.
- Teammate-376's CI probe ran in the background; reported green ~45 min after PR creation.
- `feature-end` for #376 was executed autonomously by teammate-376 (not relayed by lead) — process deviation, but produced correct output.

## What worked / what didn't

**Worked:**
- Parallel execution was clean — no file conflicts across the three branches.
- Teammate-358 caught the atomicity gap independently during its own PR review cycle; no lead intervention needed.
- CI green on all three PRs before any feature-end was triggered.

**Didn't work:**
- Teammate-377 context exhaustion: a 3-hour session window is too long. `/feature-end` should be triggered promptly after PR approval, not hours later.
- Issue #377 body had a factually incorrect claim ("PollingStatusBadge only used from My Assessments") — the teammate followed it faithfully and shipped a regression. LLD prescriptions and issue body claims about component usage scope need verification against the codebase.

## Process notes for `/retro`

1. **Long teammate sessions risk context crash.** Set a soft limit: if a teammate's session exceeds ~90 min without a feature-end signal, prompt the user to review the PR sooner. Crash recovery works but costs ~2× the original session.
2. **Issue body "only used in X" claims should be verified by grep.** Add a note to `/bug` and `/feature-core` to grep for component/function usage before accepting scope characterisations in the issue body.
3. **`PollingStatusBadge` usage scope** is now documented in `lld-nav-results.md` §1 (issue #377 implementation note). Future changes to this component should read that note.
