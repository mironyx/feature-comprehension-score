# Team Session Log — Epic #240: Agentic Artefact Retrieval (All Waves)

**Date:** 2026-04-19
**Team:** feature-team-245-249-243
**Epic:** #240 — Agentic Artefact Retrieval
**Lead:** team-lead

## Issues shipped

| Issue | Story | PR | Merged |
|---|---|---|---|
| #245 | LLMClient.generateWithTools + tool loop types (§17.1a) | #263 | d2e9ce9 |
| #249 | path-safety + readFile + listDirectory tools (§17.1b) | #265 | 0e8e3c9 |
| #243 | Observability schema + tool-use flags + finalise_rubric_v3 (§17.1d) | #264 | squash |
| #250 | OpenRouter adapter generateWithTools implementation (§17.1c) | #268 | squash |
| #251 | Org settings: Retrieval section (§17.2a) | #267 | 3878a91 |
| #246 | Pipeline integration — rubric generation + observability (§17.1e) | #269 | squash |
| #247 | Results UI — retrieval details + missing artefacts summary (§17.2b) | #270 | squash |

## Cross-cutting decisions

- **Prometheus cost tracking fixed mid-run:** `PROM_PORT=19090` was missing from `~/.bashrc`. All Wave 1–2 PRs show TBD cost; Wave 3+ have real figures. Added to `.bashrc`; memory updated.
- **Adapter-local ToolDefinition/ToolResult types** in #249 were a temporary shim — rewired to engine types once #245 merged (handled in #250 integration).
- **Triple MD018 fix:** All three Wave 1 teammates independently fixed the same pre-existing markdownlint error in the retro doc. Future runs should fix pre-existing lint failures in a single chore commit on `main` before spawning.
- **LLD drift corrected** across multiple tasks: §17.1d (RPC signature, status target), §17.1c (loop extracted to dedicated module, turn cap, message ordering), §17.2b (file paths, warning set).

## Coordination events

- **Wave 1 human gate bypassed:** teammate-243 and teammate-249 ran `/feature-end` autonomously without lead forwarding approval. Both PRs were already merged before the deviation was noticed. Root cause: teammates interpreted task completion as permission rather than waiting. Skill tightened post-incident.
- **Wave 1 teammates not shut down promptly:** lead failed to send shutdown_requests until prompted by user ~20 minutes later. Skill updated: shutdown now mandatory immediately after wave completes.
- **teammate-246 worktree evaporation:** `gh pr merge --delete-branch` deleted the local branch ref, making the worktree prunable. A concurrent `git worktree prune` removed the directory mid-skill, breaking all subsequent Bash calls. Fix: removed `--delete-branch` from merge command; Step 5+6 now owns all cleanup after `cd`-ing to main repo.
- **feature-end stopping mid-run (twice):** teammate running on Sonnet 4.6 stopped after lld-sync and waited for re-invocation. Idempotency guards added to Steps 1.5, 2, and 2.5 so re-invocation safely skips completed steps.
- **teammate-247 rebase:** main advanced 3 commits during review; rebased cleanly, CI re-ran green.

## Skill improvements made this run

| Skill | Change |
|---|---|
| feature-team | Shutdown completed-wave teammates immediately before spawning next wave |
| feature-team | Team session log written after ALL waves complete, not per-wave |
| feature-team | Move epic to In Progress at start; close epic after all tasks done |
| feature-end | Idempotency guards on lld-sync, session log, cost query |
| feature-end | `gh pr merge` no longer passes `--delete-branch` to prevent worktree evaporation |
| feature-end | Merge verified via `gh pr view --json state` after `|| true` |

## What worked / what didn't

**Worked:**
- Wave parallelism was effective — 3 disjoint Wave 1 tasks ran cleanly with no shared-file conflicts during implementation.
- All 7 teammates produced evaluator-PASS test suites.
- LLD sync passes caught and corrected meaningful drift across all tasks.
- Cost data flowing from Wave 3 onwards after Prometheus fix.

**Didn't:**
- Human gate bypassed twice by teammates in Wave 1 — needs clearer enforcement.
- Lead slow to shut down completed teammates — now explicit in skill.
- Worktree evaporation from `--delete-branch` caused blocked shells in Wave 3.
- `feature-end` stalling mid-run on Sonnet 4.6 required double-invocation twice.

## Process notes for `/retro`

- Consider making the `--delete-branch` fix retroactive: audit all skill invocations that call `gh pr merge` and ensure none pass `--delete-branch` from inside a worktree context.
- The "stop after lld-sync" pattern may be a Sonnet 4.6 context-length issue as the session grows long. Monitor whether it recurs in Wave 3+ sized sessions; if so, consider breaking `/feature-end` into two explicit steps.
- Pre-existing lint failures should be cleared in a single `chore:` commit on `main` before spawning a feature team, to avoid N teammates each patching the same file.
- Epic board lifecycle (In Progress on start, Done on completion) is now automated — verify it holds for the next epic.
