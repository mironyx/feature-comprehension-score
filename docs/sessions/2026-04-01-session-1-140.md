# Session Log — 2026-04-01, Session 1

**Issue:** #140 — Add organisation context for client prompt customisation
**Branch:** `feat/org-context`
**PR:** [#159](https://github.com/leonids2005/feature-comprehension-score/pull/159)

---

## Work completed

- Implemented all 4 tasks from LLD `docs/design/lld-organisation-context.md`:
  1. **DB schema + migration** — `organisation_contexts` table with `UNIQUE NULLS NOT DISTINCT`,
     RLS (members read, admins write), generated migration via `supabase db diff`.
  2. **Engine types** — `OrganisationContextSchema` (4 optional fields), extended
     `AssembledArtefactSet` with optional `organisation_context`.
  3. **Prompt builder** — `formatOrganisationContext` decomposed into 5 helpers for CodeScene
     compliance (cc ≤ 9). Inserted in `formatUserPrompt` after assessment context, before PR
     description.
  4. **Supabase adapter** — `loadOrgPromptContext` loads and validates org-level row, wired into
     `triggerRubricGeneration` via `Promise.all` for parallel fetch.
- 18 new tests (407 total), all passing.
- PR review (`/pr-review-v2`) found 2 issues, both fixed:
  - Silent swallow in `loadOrgPromptContext` — added `logger.warn`.
  - Missing justification comments on decomposed helpers.
- LLD synced (v0.1 → v0.2): 3 corrections, 3 additions, 0 omissions.

## Decisions made

- **`project_id` FK removed** — `projects` table doesn't exist yet. Kept as bare `uuid` with
  comment; FK to be added in V2.
- **Supabase CLI grant statements** — `db diff` (v2.77) auto-generates default grants for
  anon/authenticated/service_role. Left as-is to avoid manual migration editing.
- **`Promise.all` for parallel fetch** — `extractFromPRs` and `loadOrgPromptContext` are
  independent; running in parallel is a free performance win.

## Cost retrospective

| Metric | At PR creation | Final | Delta |
| ------ | -------------- | ----- | ----- |
| Cost | $5.23 | $8.73 | +$3.50 |
| Output tokens | 20,465 | 44,571 | +24,106 |
| Cache-write | 103,011 | 381,346 | +278,335 |

**Cost drivers:**

- **Context compaction** — session hit context limit, triggering a compact + continuation.
  This inflated cache-write tokens significantly (+278K).
- **PR review agents** (2 agents) — re-sent full diff to each subagent.
- **User Q&A after PR** — several rounds of questions about the migration file, index types,
  and Supabase query patterns added to the post-PR token spend.

**Improvement actions:**

- This feature was ~140 lines of implementation — well within the 200-line target. The cost
  driver was the extended review conversation, not implementation complexity.
- Context compaction could be avoided by keeping the post-PR conversation brief and running
  `/feature-end` promptly after approval.

## Next steps

- #157 — API write path (`PATCH /api/organisations/[id]/context`) — unblocked by this PR
- #158 — Settings UI for organisation context — unblocked by this PR
